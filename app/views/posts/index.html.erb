<h1>Posts</h1>

<ul>
    <% @posts.each do |post| %>
        <li><%= post.title %> | <%= post.body %> | posted <%= time_ago_in_words post.created_at %> ago</li>
    <% end %>
</ul>

<%= link_to "Write a new post", new_post_path %>